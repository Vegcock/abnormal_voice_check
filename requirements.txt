# 音频异常检测服务器依赖包
# GitHub: https://github.com/Vegcock/abnormal_voice_check.git
# 
# 安装方法:
#   pip install -r requirements.txt
#
# 注意: PyTorch需要根据您的系统选择CPU或GPU版本
# CPU版本: pip install torch --index-url https://download.pytorch.org/whl/cpu
# GPU版本: 访问 https://pytorch.org/ 获取适合您CUDA版本的安装命令

# Web框架
flask>=2.0.0
flask-cors>=3.0.0
werkzeug>=2.0.0

# 深度学习框架
# 注意: torch需要单独安装，请根据您的系统选择CPU或GPU版本
# CPU版本安装命令: pip install torch --index-url https://download.pytorch.org/whl/cpu
# 或者使用conda: conda install pytorch cpuonly -c pytorch
torch>=1.10.0

# 数值计算和科学计算
numpy>=1.21.0
scipy>=1.7.0

# 音频处理
librosa>=0.9.0
soundfile>=0.10.0

# 机器学习
scikit-learn>=1.0.0

# 进度条
tqdm>=4.62.0

# 数据可视化（用于训练和特征分析，服务器运行时可选）
matplotlib>=3.5.0
seaborn>=0.11.0

